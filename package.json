{
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.7",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "vitest": "^3.0.7"
  },
  "scripts": {
    "test": "vitest --coverage",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx}": [
      "vitest related"
    ]
  }
}
